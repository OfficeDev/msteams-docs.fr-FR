---
title: Qu’est-ce qu’un module de tâches ?
author: clearab
description: Ajoutez des expériences contextuelles modales pour collecter ou afficher des informations pour vos utilisateurs à partir de vos applications Microsoft Teams.
ms.topic: overview
ms.author: anclear
ms.openlocfilehash: 22fdc7a9dab1ff6f27e2b0d144e54676b6cca50e
ms.sourcegitcommit: 4329a94918263c85d6c65ff401f571556b80307b
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 02/01/2020
ms.locfileid: "41673974"
---
# <a name="what-are-task-modules"></a><span data-ttu-id="f3542-103">Qu’est-ce qu’un module de tâches ?</span><span class="sxs-lookup"><span data-stu-id="f3542-103">What are task modules?</span></span>

<span data-ttu-id="f3542-104">Les modules de tâches vous permettent de créer des expériences de menu contextuel modal dans votre application Teams.</span><span class="sxs-lookup"><span data-stu-id="f3542-104">Task modules allow you to create modal popup experiences in your Teams application.</span></span> <span data-ttu-id="f3542-105">À l’intérieur de la fenêtre contextuelle, vous pouvez exécuter votre propre code HTML/ `<iframe>`JavaScript personnalisé, afficher un widget basé sur une vidéo YouTube ou une vidéo Microsoft Stream, ou afficher une [carte adaptative](/adaptive-cards/).</span><span class="sxs-lookup"><span data-stu-id="f3542-105">Inside the popup you can run your own custom HTML/JavaScript code, show an `<iframe>`-based widget such as a YouTube or Microsoft Stream video or display an [Adaptive card](/adaptive-cards/).</span></span> <span data-ttu-id="f3542-106">Elles sont particulièrement utiles pour lancer et effectuer des tâches ou afficher des informations enrichies, telles que des vidéos ou des tableaux de bord Power BI.</span><span class="sxs-lookup"><span data-stu-id="f3542-106">They are especially useful for initiating and completing tasks or displaying rich information like videos or Power BI dashboards.</span></span> <span data-ttu-id="f3542-107">Une expérience contextuelle est souvent plus naturelle pour les utilisateurs qui initient et effectuent des tâches par rapport à un onglet ou à une expérience de robot basée sur une conversation.</span><span class="sxs-lookup"><span data-stu-id="f3542-107">A popup experience is often more natural for users initiating and completing tasks compared to a tab or a conversation-based bot experience.</span></span>

<span data-ttu-id="f3542-108">Les modules de tâches sont présentés sous la base des onglets Microsoft teams ; Il s’agit essentiellement d’un onglet à l’intérieur d’une fenêtre contextuelle.</span><span class="sxs-lookup"><span data-stu-id="f3542-108">Task modules build on the foundation of Microsoft Teams tabs; they are essentially a tab inside a popup window.</span></span> <span data-ttu-id="f3542-109">Ils utilisent le même Kit de développement logiciel (SDK), donc si vous avez créé un onglet, vous avez déjà 90% de la façon de créer un module de tâches.</span><span class="sxs-lookup"><span data-stu-id="f3542-109">They use the same SDK, so if you've built a tab you are already 90% of the way to being able to create a task module.</span></span>

<span data-ttu-id="f3542-110">Les modules de tâches peuvent être appelés de trois façons :</span><span class="sxs-lookup"><span data-stu-id="f3542-110">Task modules can be invoked in three ways:</span></span>

* <span data-ttu-id="f3542-111">**Onglets de canal ou personnel.**</span><span class="sxs-lookup"><span data-stu-id="f3542-111">**Channel or personal tabs.**</span></span> <span data-ttu-id="f3542-112">À l’aide du kit de développement Microsoft Teams, vous pouvez appeler des modules de tâches à partir des boutons, des liens ou des menus de votre onglet. [cette information est décrite en détail ici.](~/task-modules-and-cards/task-modules/task-modules-tabs.md)</span><span class="sxs-lookup"><span data-stu-id="f3542-112">Using the Microsoft Teams Tabs SDK you can invoke task modules from buttons, links or menus on your tab. [This is covered in detail here.](~/task-modules-and-cards/task-modules/task-modules-tabs.md)</span></span>
* <span data-ttu-id="f3542-113">**Bots.**</span><span class="sxs-lookup"><span data-stu-id="f3542-113">**Bots.**</span></span> <span data-ttu-id="f3542-114">Boutons sur les [cartes](~/task-modules-and-cards/cards/cards-reference.md) envoyées à partir de votre robot.</span><span class="sxs-lookup"><span data-stu-id="f3542-114">Buttons on [cards](~/task-modules-and-cards/cards/cards-reference.md) sent from your bot.</span></span> <span data-ttu-id="f3542-115">Ceci est particulièrement utile lorsque vous n’avez pas besoin de tous les utilisateurs d’un canal pour voir ce que vous faites avec un bot.</span><span class="sxs-lookup"><span data-stu-id="f3542-115">This is particularly useful when you don't need everyone in a channel to see what you are doing with a bot.</span></span> <span data-ttu-id="f3542-116">Par exemple, lorsque les utilisateurs répondent à un sondage dans un canal, il n’est pas très utile de voir un enregistrement de cette interrogation en cours de création.</span><span class="sxs-lookup"><span data-stu-id="f3542-116">For example, when having users respond to a poll in a channel it's not terribly useful to see a record of that poll being created.</span></span> [<span data-ttu-id="f3542-117">Cette information est décrite en détail ici.</span><span class="sxs-lookup"><span data-stu-id="f3542-117">This is covered in detail here.</span></span>](~/task-modules-and-cards/task-modules/task-modules-bots.md)
* <span data-ttu-id="f3542-118">**En dehors de teams à partir d’un lien profond.**</span><span class="sxs-lookup"><span data-stu-id="f3542-118">**Outside of Teams from a deep link.**</span></span> <span data-ttu-id="f3542-119">Vous pouvez également créer des URL pour appeler un module de tâches depuis n’importe quel endroit.</span><span class="sxs-lookup"><span data-stu-id="f3542-119">You can also create URLs to invoke a task module from anywhere.</span></span> [<span data-ttu-id="f3542-120">Cette information est décrite en détail ici.</span><span class="sxs-lookup"><span data-stu-id="f3542-120">This is covered in detail here.</span></span>](#task-module-deep-link-syntax)

## <a name="what-a-task-module-looks-like"></a><span data-ttu-id="f3542-121">À quoi ressemble un module de tâche ?</span><span class="sxs-lookup"><span data-stu-id="f3542-121">What a task module looks like</span></span>

<span data-ttu-id="f3542-122">Voici à quoi ressemble un module de tâche lorsqu’il est appelé à partir d’un bot (sans les rectangles de couleur et les cercles numérotés, bien entendu) :</span><span class="sxs-lookup"><span data-stu-id="f3542-122">Here's what a task module looks like when invoked from a bot (without the colored rectangles and numbered circles, of course):</span></span>

![Exemple de module de tâche](~/assets/images/task-module/task-module-example.png)

<span data-ttu-id="f3542-124">Passons en revue les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="f3542-124">Let's walk through it:</span></span>

1. <span data-ttu-id="f3542-125">[ `color` Icône](~/resources/schema/manifest-schema.md#icons)de votre application.</span><span class="sxs-lookup"><span data-stu-id="f3542-125">Your app's [`color` icon](~/resources/schema/manifest-schema.md#icons).</span></span>
2. <span data-ttu-id="f3542-126">[ `short` Nom](~/resources/schema/manifest-schema.md#name)de votre application.</span><span class="sxs-lookup"><span data-stu-id="f3542-126">Your app's [`short` name](~/resources/schema/manifest-schema.md#name).</span></span>
3. <span data-ttu-id="f3542-127">Titre du module de tâche spécifié dans la `title` propriété de l' [objet TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="f3542-127">The task module's title specified in the `title` property of the [TaskInfo object](#the-taskinfo-object).</span></span>
4. <span data-ttu-id="f3542-128">Bouton Fermer/annuler du module tâches.</span><span class="sxs-lookup"><span data-stu-id="f3542-128">The task module's close/cancel button.</span></span> <span data-ttu-id="f3542-129">Si l’utilisateur appuie sur cette touche, votre application reçoit un `err` événement tel que décrit [ici](~/task-modules-and-cards/task-modules/task-modules-tabs.md#example-submitting-the-result-of-a-task-module).</span><span class="sxs-lookup"><span data-stu-id="f3542-129">If the user presses this, your app will receive an `err` event as described [here](~/task-modules-and-cards/task-modules/task-modules-tabs.md#example-submitting-the-result-of-a-task-module).</span></span> <span data-ttu-id="f3542-130">(**Remarque :** il n’est pas possible de détecter cet événement actuellement lorsqu’un module de tâche est appelé à partir d’un bot.)</span><span class="sxs-lookup"><span data-stu-id="f3542-130">(**Note:** It is currently not possible to detect this event when a task module is invoked from a bot.)</span></span>
5. <span data-ttu-id="f3542-131">Le rectangle bleu est l’emplacement où votre page Web apparaît si vous chargez votre page Web à l' `url` aide de la propriété de l' [objet TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="f3542-131">The blue rectangle is the where your web page appears if you are loading your own web page using the `url` property of the [TaskInfo object](#the-taskinfo-object).</span></span> <span data-ttu-id="f3542-132">Vous trouverez plus de détails dans la section [dimensionnement du module tâches](#task-module-sizing) ci-dessous.</span><span class="sxs-lookup"><span data-stu-id="f3542-132">More detail is in the [task module sizing](#task-module-sizing) section below.</span></span>
6. <span data-ttu-id="f3542-133">Si vous affichez une carte adaptative via la `card` propriété de l' [objet TaskInfo](#the-taskinfo-object) auquel le remplissage est ajouté, dans le cas contraire, vous devrez le [gérer vous-même](#task-module-css-for-htmljavascript-task-modules).</span><span class="sxs-lookup"><span data-stu-id="f3542-133">If you are displaying an Adaptive card via the `card` property of the [TaskInfo object](#the-taskinfo-object) the padding is added for you, otherwise you'll need to [handle this yourself](#task-module-css-for-htmljavascript-task-modules).</span></span>
7. <span data-ttu-id="f3542-134">Les boutons de carte adaptative s’affichent ici.</span><span class="sxs-lookup"><span data-stu-id="f3542-134">Adaptive card buttons will render here.</span></span> <span data-ttu-id="f3542-135">Si vous utilisez votre propre page, vous devez créer vos propres boutons.</span><span class="sxs-lookup"><span data-stu-id="f3542-135">If you're using your own page you must create your own buttons.</span></span>

## <a name="overview-of-invoking-and-dismissing-task-modules"></a><span data-ttu-id="f3542-136">Vue d’ensemble des modules de tâches d’appel et de disparition</span><span class="sxs-lookup"><span data-stu-id="f3542-136">Overview of invoking and dismissing task modules</span></span>

<span data-ttu-id="f3542-137">Les modules de tâches peuvent être appelés à partir de tabulations, robots ou liens détaillés et ce qui apparaît dans un peut être soit du code HTML, soit une carte adaptative, de sorte qu’il dispose de beaucoup de flexibilité quant à l’appel et à la façon de traiter le résultat de l’interaction d’un utilisateur.</span><span class="sxs-lookup"><span data-stu-id="f3542-137">Task modules can be invoked from tabs, bots or deep links and what appears in one can be either HTML or an Adaptive card, so there's a lot of flexibility in terms of how they are invoked and how to deal with the result of a user's interaction.</span></span> <span data-ttu-id="f3542-138">Le tableau ci-dessous récapitule le fonctionnement :</span><span class="sxs-lookup"><span data-stu-id="f3542-138">The table below summarizes how this works:</span></span>

| <span data-ttu-id="f3542-139">**Appelé via...**</span><span class="sxs-lookup"><span data-stu-id="f3542-139">**Invoked via...**</span></span> | <span data-ttu-id="f3542-140">**Le module de tâches est HTML/JavaScript**</span><span class="sxs-lookup"><span data-stu-id="f3542-140">**Task module is HTML/JavaScript**</span></span> | <span data-ttu-id="f3542-141">**Le module de tâche est une carte adaptative**</span><span class="sxs-lookup"><span data-stu-id="f3542-141">**Task module is Adaptive card**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="f3542-142">**JavaScript dans un onglet**</span><span class="sxs-lookup"><span data-stu-id="f3542-142">**JavaScript in a tab**</span></span> | <span data-ttu-id="f3542-143">1. utiliser la fonction `tasks.startTask()` de kit de développement logiciel client `submitHandler(err, result)` teams avec une fonction de rappel facultative</span><span class="sxs-lookup"><span data-stu-id="f3542-143">1. Use the Teams client SDK function `tasks.startTask()` with an optional `submitHandler(err, result)` callback function</span></span> <br/><br/> <span data-ttu-id="f3542-144">2. dans le code du module de tâche, lorsque l’utilisateur a terminé, appelez la fonction `tasks.submitTask()` Kit de `result` développement logiciel (SDK) teams avec un objet en tant que paramètre.</span><span class="sxs-lookup"><span data-stu-id="f3542-144">2. In the task module code, when the user is finished, call the Teams SDK function `tasks.submitTask()` with a `result` object as a parameter.</span></span> <span data-ttu-id="f3542-145">Si un `submitHandler` rappel a été spécifié `tasks.startTask()`dans, teams l' `result` appelle en tant que paramètre.</span><span class="sxs-lookup"><span data-stu-id="f3542-145">If a `submitHandler` callback was specified in `tasks.startTask()`, Teams calls it with `result` as a parameter.</span></span><br/><br/> <span data-ttu-id="f3542-146">3. Si une erreur s’est produite lors de `tasks.startTask()`l’appel `submitHandler` , la fonction est appelée `err` avec une chaîne à la place.</span><span class="sxs-lookup"><span data-stu-id="f3542-146">3. If there was an error when invoking `tasks.startTask()`, the `submitHandler` function is called with an `err` string instead.</span></span> <br/><br/> <span data-ttu-id="f3542-147">4. vous pouvez également spécifier un `completionBotId` lors de `teams.startTask()` l’appel, ce `result` qui est envoyé au robot à la place.</span><span class="sxs-lookup"><span data-stu-id="f3542-147">4. You can also specify a `completionBotId` when calling `teams.startTask()` - in that case `result` is sent to the bot instead.</span></span> | <span data-ttu-id="f3542-148">1. appelez la fonction `tasks.startTask()` de kit de développement logiciel client teams avec un [objet TaskInfo](#the-taskinfo-object) et `TaskInfo.card` contenant le JSON de la carte adaptative à afficher dans le menu contextuel du module de tâche.</span><span class="sxs-lookup"><span data-stu-id="f3542-148">1. Call the Teams client SDK function `tasks.startTask()` with a [TaskInfo object](#the-taskinfo-object) and `TaskInfo.card` containing the JSON for the Adaptive card to show in the task module popup.</span></span> <br/><br/> <span data-ttu-id="f3542-149">2. Si un `submitHandler` rappel a été spécifié `tasks.startTask()`dans, teams l’appelle `err` avec une chaîne si une erreur s’est produite `tasks.startTask()` lors de l’appel ou si l’utilisateur ferme le menu contextuel du module de tâche en utilisant le X en haut à droite.</span><span class="sxs-lookup"><span data-stu-id="f3542-149">2. If a `submitHandler` callback was specified in `tasks.startTask()`, Teams calls it with an `err` string if there was an error when invoking `tasks.startTask()` or if the user closes the task module popup using the X at the upper right.</span></span> <br/><br/> <span data-ttu-id="f3542-150">3. si l’utilisateur appuie sur un bouton action. Submit, son `data` objet est renvoyé comme valeur de `result`.</span><span class="sxs-lookup"><span data-stu-id="f3542-150">3. If the user presses an Action.Submit button then its `data` object is returned as the value of `result`.</span></span> |
| <span data-ttu-id="f3542-151">**Bouton de carte bot**</span><span class="sxs-lookup"><span data-stu-id="f3542-151">**Bot card button**</span></span> | <span data-ttu-id="f3542-152">1 les boutons de carte. bot, selon le type de bouton, peuvent appeler des modules de tâches de deux manières : une URL de lien hypertexte ou `task/fetch` l’envoi d’un message.</span><span class="sxs-lookup"><span data-stu-id="f3542-152">1. Bot card buttons, depending on the type of button, can invoke task modules in two ways: a deep link URL or by sending a `task/fetch` message.</span></span> <span data-ttu-id="f3542-153">Voir ci-dessous pour savoir comment fonctionnent les URL de liens.</span><span class="sxs-lookup"><span data-stu-id="f3542-153">See below for how deep link URLs work.</span></span> <br/><br/> <span data-ttu-id="f3542-154">2. Si `type` l’action du bouton est `task/fetch` (`Action.Submit` type de bouton pour les cartes adaptatives) `task/fetch invoke` , un événement (un billet http sous les couvertures) est envoyé au bot, et le bot répond au billet avec http 200 et le corps de la réponse contenant un wrapper entourant l' [objet TaskInfo](#the-taskinfo-object).</span><span class="sxs-lookup"><span data-stu-id="f3542-154">2. If the button's action `type` is `task/fetch` (`Action.Submit` button type for Adaptive cards), a `task/fetch invoke` event (an HTTP POST under the covers) is sent to the bot, and the bot responds to the POST with HTTP 200 and the response body containing a wrapper around the [TaskInfo object](#the-taskinfo-object).</span></span> <span data-ttu-id="f3542-155">Cette étape est expliquée en détail dans l' [appel d’un module de tâches via Task/FETCH](~/task-modules-and-cards/task-modules/task-modules-bots.md#invoking-a-task-module-via-taskfetch).</span><span class="sxs-lookup"><span data-stu-id="f3542-155">This is explained in detail in [invoking a task module via task/fetch](~/task-modules-and-cards/task-modules/task-modules-bots.md#invoking-a-task-module-via-taskfetch).</span></span><br/><br/> <span data-ttu-id="f3542-156">3. teams affiche le module tâches ; une fois l’utilisateur terminé, appelez la fonction `tasks.submitTask()` Team SDK avec un `result` objet en tant que paramètre.</span><span class="sxs-lookup"><span data-stu-id="f3542-156">3. Teams displays the task module; when the user is finished, call the Teams SDK function `tasks.submitTask()` with a `result` object as a parameter.</span></span> <br/><br/> <span data-ttu-id="f3542-157">4. le bot reçoit un `task/submit invoke` message qui contient l' `result` objet.</span><span class="sxs-lookup"><span data-stu-id="f3542-157">4. The bot receives a `task/submit invoke` message that contains the `result` object.</span></span> <span data-ttu-id="f3542-158">Vous disposez de trois façons différentes de répondre au `task/submit` message : en ne faisant rien (la tâche s’est terminée correctement), en affichant un message pour l’utilisateur dans une fenêtre contextuelle ou en appelant une autre fenêtre de module de tâche (c’est-à-dire en créant une expérience de type assistant).</span><span class="sxs-lookup"><span data-stu-id="f3542-158">You have three different ways to respond to the `task/submit` message: by doing nothing (the task completed successfully), by displaying a message to the user in a popup window, or by invoking another task module window (i.e. creating a wizard-like experience).</span></span> <span data-ttu-id="f3542-159">Ces trois options sont décrites plus [en détail dans la rubrique discussion détaillée sur la tâche/l’envoi](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span><span class="sxs-lookup"><span data-stu-id="f3542-159">These three options are discussed more [in the detailed discussion on task/submit](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span></span> | <span data-ttu-id="f3542-160">1. comme les boutons sur les cartes de l’infrastructure bot, les boutons sur les cartes adaptatives prennent en charge deux façons d’appeler `Action.openUrl` des modules de tâches `task/fetch` : `Action.Submit` URL de liens en profondeur avec boutons et via à l’aide de boutons.</span><span class="sxs-lookup"><span data-stu-id="f3542-160">1. Like buttons on Bot Framework cards, buttons on Adaptive cards support two ways of invoking task modules: deep link URLs with `Action.openUrl` buttons, and via `task/fetch` using `Action.Submit` buttons.</span></span> <br/><br/> <span data-ttu-id="f3542-161">2. les modules de tâches avec cartes adaptatives fonctionnent de façon très similaire à la casse HTML/JavaScript (voir Left).</span><span class="sxs-lookup"><span data-stu-id="f3542-161">2. Task modules with Adaptive cards work very similarly to the HTML/JavaScript case (see left).</span></span> <span data-ttu-id="f3542-162">La principale différence est qu’étant donné qu’il n’y a aucun code JavaScript lorsque vous utilisez des cartes adaptatives, il `tasks.submitTask()`n’existe aucun moyen d’appeler.</span><span class="sxs-lookup"><span data-stu-id="f3542-162">The major difference is that since there's no JavaScript when you're using Adaptive cards, there's no way to call `tasks.submitTask()`.</span></span> <span data-ttu-id="f3542-163">En revanche, teams `data` prend l' `Action.Submit` objet de et le renvoie comme charge utile de `task/submit` dans l’événement, comme décrit [ici](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span><span class="sxs-lookup"><span data-stu-id="f3542-163">Instead, Teams takes the `data` object from `Action.Submit` and returns it as the payload of in the `task/submit` event, as described [here](~/task-modules-and-cards/task-modules/task-modules-bots.md#the-flexibility-of-tasksubmit).</span></span> |
| <span data-ttu-id="f3542-164">**URL de liens détaillés**</span><span class="sxs-lookup"><span data-stu-id="f3542-164">**Deep link URL**</span></span> <br/>[<span data-ttu-id="f3542-165">Syntaxe d'URL</span><span class="sxs-lookup"><span data-stu-id="f3542-165">URL syntax</span></span>](#task-module-deep-link-syntax) | <span data-ttu-id="f3542-166">1. teams appelle le module tâches ; URL qui apparaît à l’intérieur `<iframe>` du paramètre spécifié `url` dans le paramètre du lien profond.</span><span class="sxs-lookup"><span data-stu-id="f3542-166">1. Teams invokes the task module; the URL that appears inside the `<iframe>` specified in the `url` parameter of the deep link.</span></span> <span data-ttu-id="f3542-167">Il n’y `submitHandler` a pas de rappel.</span><span class="sxs-lookup"><span data-stu-id="f3542-167">There is no `submitHandler` callback.</span></span> <br/><br/> <span data-ttu-id="f3542-168">2. dans le code JavaScript de la page dans le module de tâche `tasks.submitTask()` , appelez pour le fermer `result` avec un objet en tant que paramètre, de la même manière que lors de son appel à partir d’un bouton de la carte ou d’un bouton de carte de robot.</span><span class="sxs-lookup"><span data-stu-id="f3542-168">2. Within the JavaScript of the page in the task module, call `tasks.submitTask()` to close it with a `result` object as a parameter, the same as when invoking it from a tab or a bot card button.</span></span> <span data-ttu-id="f3542-169">La logique de fin est légèrement différente.</span><span class="sxs-lookup"><span data-stu-id="f3542-169">Completion logic is slightly different, however.</span></span> <span data-ttu-id="f3542-170">Si votre logique de saisie réside sur le client (c’est-à-dire s’il n’y a aucun bot), il n' `submitHandler` y a aucun rappel, de sorte que toute logique d’achèvement doit se trouver `tasks.submitTask()`dans le code précédant l’appel à.</span><span class="sxs-lookup"><span data-stu-id="f3542-170">If your completion logic resides on the client (i.e. if there is no bot) there is no `submitHandler` callback, so any completion logic must be in the code preceding the call to `tasks.submitTask()`.</span></span> <span data-ttu-id="f3542-171">Les erreurs d’invocation sont uniquement signalées via la console.</span><span class="sxs-lookup"><span data-stu-id="f3542-171">Invocation errors are only reported via the console.</span></span> <span data-ttu-id="f3542-172">Si vous avez un bot, vous pouvez spécifier un `completionBotId` paramètre dans le lien profond pour envoyer l' `result` objet via un `task/submit` événement.</span><span class="sxs-lookup"><span data-stu-id="f3542-172">If you have a bot, then you can specify a `completionBotId` parameter in the deep link to send the `result` object via a `task/submit` event.</span></span> | <span data-ttu-id="f3542-173">1. teams appelle le module tâches ; le corps de la carte JSON de la carte adaptative est spécifié en tant que valeur codée URL du `card` paramètre du lien profond.</span><span class="sxs-lookup"><span data-stu-id="f3542-173">1. Teams invokes the task module; the JSON card body of the Adaptive card is specified as a URL-encoded value of the `card` parameter of the deep link.</span></span> <br/><br/> <span data-ttu-id="f3542-174">2. l’utilisateur ferme le module de tâche en cliquant sur le X dans le coin supérieur droit du module de tâches ou en `Action.Submit` appuyant sur un bouton de la carte.</span><span class="sxs-lookup"><span data-stu-id="f3542-174">2. The user closes the task module by clicking on the X at the upper right of the task module or by pressing an `Action.Submit` button on the card.</span></span> <span data-ttu-id="f3542-175">Étant donné qu’il `submitHandler` n’y a pas à appeler, vous devez disposer d’un bot pour envoyer la valeur des champs de carte adaptative à.</span><span class="sxs-lookup"><span data-stu-id="f3542-175">Since there is no `submitHandler` to call, you must have a bot to send the value of the Adaptive card fields to.</span></span> <span data-ttu-id="f3542-176">Vous utilisez le `completionBotId` paramètre dans le lien profond pour spécifier le bot auquel envoyer les données via un `task/submit invoke` événement.</span><span class="sxs-lookup"><span data-stu-id="f3542-176">You use the `completionBotId` parameter in the deep link to specify the bot to send the data to via a `task/submit invoke` event.</span></span> |

> [!NOTE]
> <span data-ttu-id="f3542-177">L’appel d’un module de tâches à partir de JavaScript n’est pas pris en charge sur mobile.</span><span class="sxs-lookup"><span data-stu-id="f3542-177">Invoking a task module from JavaScript is not supported on mobile.</span></span>

## <a name="the-taskinfo-object"></a><span data-ttu-id="f3542-178">L’objet TaskInfo</span><span class="sxs-lookup"><span data-stu-id="f3542-178">The TaskInfo object</span></span>

<span data-ttu-id="f3542-179">L' `TaskInfo` objet contient les métadonnées d’un module de tâche.</span><span class="sxs-lookup"><span data-stu-id="f3542-179">The `TaskInfo` object contains the metadata for a task module.</span></span> <span data-ttu-id="f3542-180">La définition de l’objet est ci-dessous.</span><span class="sxs-lookup"><span data-stu-id="f3542-180">The object definition is below.</span></span> <span data-ttu-id="f3542-181">Vous **devez** définir soit `url` (pour un IFRAME en Embedded `card` ), soit (pour une carte adaptative).</span><span class="sxs-lookup"><span data-stu-id="f3542-181">You **must** define either `url` (for en embedded iFrame) or `card` (for an Adaptive Card).</span></span>

| <span data-ttu-id="f3542-182">Attribut</span><span class="sxs-lookup"><span data-stu-id="f3542-182">Attribute</span></span> | <span data-ttu-id="f3542-183">Type</span><span class="sxs-lookup"><span data-stu-id="f3542-183">Type</span></span> | <span data-ttu-id="f3542-184">Description</span><span class="sxs-lookup"><span data-stu-id="f3542-184">Description</span></span> |
| --- | --- | --- |
| `title` | <span data-ttu-id="f3542-185">string</span><span class="sxs-lookup"><span data-stu-id="f3542-185">string</span></span> | <span data-ttu-id="f3542-186">Apparaît sous le nom de l’application et à droite de l’icône de l’application</span><span class="sxs-lookup"><span data-stu-id="f3542-186">Appears below the app name and to the right of the app icon</span></span> |
| `height` | <span data-ttu-id="f3542-187">valeur numérique ou chaîne</span><span class="sxs-lookup"><span data-stu-id="f3542-187">number or string</span></span> | <span data-ttu-id="f3542-188">Il peut s’agir d’un nombre représentant la hauteur du module de tâche en `small`pixels `medium`, ou `large`, ou.</span><span class="sxs-lookup"><span data-stu-id="f3542-188">This can be a number representing the task module's height in pixels, or `small`, `medium`, or `large`.</span></span> <span data-ttu-id="f3542-189">[Voir ci-dessous la manière dont la hauteur et la largeur sont gérées](#task-module-sizing).</span><span class="sxs-lookup"><span data-stu-id="f3542-189">[See below for how height and width are handled](#task-module-sizing).</span></span> |
| `width` | <span data-ttu-id="f3542-190">valeur numérique ou chaîne</span><span class="sxs-lookup"><span data-stu-id="f3542-190">number or string</span></span> | <span data-ttu-id="f3542-191">Il peut s’agir d’un nombre représentant la largeur du module de tâche en `small`pixels `medium`, ou `large`, ou.</span><span class="sxs-lookup"><span data-stu-id="f3542-191">This can be a number representing the task module's width in pixels, or `small`, `medium`, or `large`.</span></span> <span data-ttu-id="f3542-192">[Voir ci-dessous la manière dont la hauteur et la largeur sont gérées](#task-module-sizing).</span><span class="sxs-lookup"><span data-stu-id="f3542-192">[See below for how height and width are handled](#task-module-sizing).</span></span> |
| `url` | <span data-ttu-id="f3542-193">string</span><span class="sxs-lookup"><span data-stu-id="f3542-193">string</span></span> | <span data-ttu-id="f3542-194">URL de la page chargée en tant que `<iframe>` module dans le module de tâches.</span><span class="sxs-lookup"><span data-stu-id="f3542-194">The URL of the page loaded as an `<iframe>` inside the task module.</span></span> <span data-ttu-id="f3542-195">Le domaine de l’URL doit se trouver dans le [tableau validDomains](~/resources/schema/manifest-schema.md#validdomains) de l’application dans le manifeste de votre application.</span><span class="sxs-lookup"><span data-stu-id="f3542-195">The URL's domain must be in the app's [validDomains array](~/resources/schema/manifest-schema.md#validdomains) in your app's manifest.</span></span> |
| `card` | <span data-ttu-id="f3542-196">Carte adaptative ou pièce jointe de carte de robot de carte adaptative</span><span class="sxs-lookup"><span data-stu-id="f3542-196">Adaptive card or an Adaptive card bot card attachment</span></span> | <span data-ttu-id="f3542-197">JSON de la carte adaptative à apparaître dans le module de tâches.</span><span class="sxs-lookup"><span data-stu-id="f3542-197">The JSON for the Adaptive card to appear in the task module.</span></span> <span data-ttu-id="f3542-198">Si vous appelez à partir d’un bot, vous devrez utiliser le JSON de la carte adaptative dans un objet de l' `attachment` infrastructure bot.</span><span class="sxs-lookup"><span data-stu-id="f3542-198">If you're invoking from a bot, you'll need to use the Adaptive card JSON in a Bot Framework `attachment` object.</span></span> <span data-ttu-id="f3542-199">À partir d’un onglet, vous n’utiliserez qu’une carte adaptative.</span><span class="sxs-lookup"><span data-stu-id="f3542-199">From a tab you'll use just an Adaptive Card.</span></span> [<span data-ttu-id="f3542-200">Voici un exemple.</span><span class="sxs-lookup"><span data-stu-id="f3542-200">Here's an example.</span></span>](#adaptive-card-or-adaptive-card-bot-card-attachment) |
| `fallbackUrl` | <span data-ttu-id="f3542-201">string</span><span class="sxs-lookup"><span data-stu-id="f3542-201">string</span></span> | <span data-ttu-id="f3542-202">Si un client ne prend pas en charge la fonctionnalité de module de tâche, cette URL est ouverte dans un onglet de navigateur.</span><span class="sxs-lookup"><span data-stu-id="f3542-202">If a client does not support the task module feature, this URL is opened in a browser tab.</span></span> |
| `completionBotId` | <span data-ttu-id="f3542-203">string</span><span class="sxs-lookup"><span data-stu-id="f3542-203">string</span></span> | <span data-ttu-id="f3542-204">Spécifie un ID d’application bot pour envoyer le résultat de l’interaction de l’utilisateur avec le module de tâche.</span><span class="sxs-lookup"><span data-stu-id="f3542-204">Specifies a bot App ID to send the result of the user's interaction with the task module to.</span></span> <span data-ttu-id="f3542-205">S’il est spécifié, le bot reçoit `task/submit invoke` un événement avec un objet JSON dans la charge utile de l’événement.</span><span class="sxs-lookup"><span data-stu-id="f3542-205">If specified, the bot will receive a `task/submit invoke` event with a JSON object in the event payload.</span></span> |

> [!NOTE]
> <span data-ttu-id="f3542-206">La fonctionnalité de module de tâche nécessite que les domaines des URL que vous souhaitez charger soient inclus dans `validDomains` le tableau dans le manifeste de votre application.</span><span class="sxs-lookup"><span data-stu-id="f3542-206">The task module feature requires that the domains of any URLs you want to load are included in the `validDomains` array in your app's manifest.</span></span>

## <a name="task-module-sizing"></a><span data-ttu-id="f3542-207">Dimensionnement du module de tâche</span><span class="sxs-lookup"><span data-stu-id="f3542-207">Task module sizing</span></span>

<span data-ttu-id="f3542-208">En utilisant des `TaskInfo.width` entiers `TaskInfo.height` pour et définira la hauteur et la largeur en pixels.</span><span class="sxs-lookup"><span data-stu-id="f3542-208">Using integers for `TaskInfo.width` and `TaskInfo.height` will set the height and width in pixels.</span></span> <span data-ttu-id="f3542-209">Toutefois, en fonction de la taille de la fenêtre de l’équipe et de la résolution de l’écran, elles seront réduites proportionnellement tout en conservant les proportions (largeur/hauteur).</span><span class="sxs-lookup"><span data-stu-id="f3542-209">However, depending on the size of the Team's window and screen resolution they will be reduced proportionally while maintaining the aspect ratio (width/height).</span></span>

<span data-ttu-id="f3542-210">Si `TaskInfo.width` et `TaskInfo.height` est `"small"`, `"medium"` ou `"large"` si la taille du rectangle rouge dans l’image ci-dessus est une proportion de l’espace disponible : 20%, 50%, 60 `width` % pour et 20%, 50%, 66 `height`% pour.</span><span class="sxs-lookup"><span data-stu-id="f3542-210">If `TaskInfo.width` and `TaskInfo.height` are `"small"`, `"medium"` or `"large"` the size of the red rectangle in the picture above is a proportion of the available space: 20%, 50%, 60% for `width` and 20%, 50%, 66% for `height`.</span></span>

<span data-ttu-id="f3542-211">Les modules de tâches appelés à partir d’un onglet peuvent être redimensionnés dynamiquement.</span><span class="sxs-lookup"><span data-stu-id="f3542-211">Task modules invoked from a tab can be dynamically resized.</span></span> <span data-ttu-id="f3542-212">Après l' `tasks.startTask()` appel, vous `tasks.updateTask(newSize)` pouvez appeler les propriétés Height et Width de l’objet NewSize conformément aux spécifications de TaskInfo (par exemple,</span><span class="sxs-lookup"><span data-stu-id="f3542-212">After calling `tasks.startTask()` you can call `tasks.updateTask(newSize)` where height and width properties on the newSize object conform to the TaskInfo spec (ex.</span></span> <span data-ttu-id="f3542-213">`{ height: 'medium', width: 'medium' }`).</span><span class="sxs-lookup"><span data-stu-id="f3542-213">`{ height: 'medium', width: 'medium' }`).</span></span>

## <a name="task-module-css-for-htmljavascript-task-modules"></a><span data-ttu-id="f3542-214">Module de tâches CSS pour les modules de tâches HTML/JavaScript</span><span class="sxs-lookup"><span data-stu-id="f3542-214">Task module CSS for HTML/JavaScript task modules</span></span>

<span data-ttu-id="f3542-215">Les modules de tâches HTML/JavaScript ont accès à toute la zone du module tâches sous l’en-tête.</span><span class="sxs-lookup"><span data-stu-id="f3542-215">HTML/JavaScript-based task modules have access to the entire area of the task module below the header.</span></span> <span data-ttu-id="f3542-216">Bien que cela offre une grande flexibilité, si vous voulez que le remplissage des bords s’aligne sur les éléments d’en-tête et éviter les barres de défilement inutiles, vous devrez fournir le CSS approprié.</span><span class="sxs-lookup"><span data-stu-id="f3542-216">While that offers a great deal of flexibility, if you want padding around the edges to align with the header elements and avoid unnecessary scrollbars you'll need to provide the right CSS.</span></span> <span data-ttu-id="f3542-217">Voici quelques exemples pour quelques cas d’utilisation.</span><span class="sxs-lookup"><span data-stu-id="f3542-217">Here are some examples for a few use cases.</span></span>

### <a name="example-1---youtube-video"></a><span data-ttu-id="f3542-218">Exemple 1 : vidéo YouTube</span><span class="sxs-lookup"><span data-stu-id="f3542-218">Example 1 - YouTube video</span></span>

<span data-ttu-id="f3542-219">YouTube offre la possibilité d’incorporer des vidéos sur des pages Web.</span><span class="sxs-lookup"><span data-stu-id="f3542-219">YouTube offers the ability to embed videos on web pages.</span></span> <span data-ttu-id="f3542-220">À l’aide d’une page Web stub simple, il est facile de l’afficher dans un module de tâches :</span><span class="sxs-lookup"><span data-stu-id="f3542-220">Using a simple stub web page it's easy to show this in a task module:</span></span>

![Vidéo YouTube](~/assets/images/task-module/youtube-example.png)

<span data-ttu-id="f3542-222">Voici le code HTML de cette page, sans la feuille de style CSS :</span><span class="sxs-lookup"><span data-stu-id="f3542-222">Here's the HTML for this page, without the CSS:</span></span>

```html
<!DOCTYPE html>
<html lang="en">
<head>
  ⋮
</head>
<body>
  <div id="embed-container">
    <iframe width="1000" height="700" src="https://www.youtube.com/embed/rd0Rd8w3FZ0" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen=""></iframe>
  </div>
</body>
</html>
```

<span data-ttu-id="f3542-223">La feuille de style CSS se présente comme suit :</span><span class="sxs-lookup"><span data-stu-id="f3542-223">The CSS looks like this:</span></span>

```css
#embed-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 95%;
    height: 95%;
    padding-left: 20px;
    padding-right: 20px;
    padding-top: 10px;
    padding-bottom: 10px;
    border-style: none;
}
```

### <a name="example-2---powerapp"></a><span data-ttu-id="f3542-224">Exemple 2 : PowerApp</span><span class="sxs-lookup"><span data-stu-id="f3542-224">Example 2 - PowerApp</span></span>

<span data-ttu-id="f3542-225">Vous pouvez également utiliser la même approche pour intégrer un PowerApp.</span><span class="sxs-lookup"><span data-stu-id="f3542-225">You can use the same approach to embed a PowerApp as well.</span></span> <span data-ttu-id="f3542-226">La hauteur et la largeur de chaque PowerApp étant personnalisables, il se peut que vous deviez ajuster la hauteur et la largeur pour obtenir la présentation souhaitée.</span><span class="sxs-lookup"><span data-stu-id="f3542-226">As the height/width of any individual PowerApp is customizable, you may need to adjust the height and width to achieve your desired presentation.</span></span>

![Gestion des biens PowerApp](~/assets/images/task-module/powerapp-example.png)

```html
<iframe width="720" height="520" src="https://web.powerapps.com/webplayer/iframeapp?source=iframe&screenColor=rgba(104,101,171,1)&appId=/providers/Microsoft.PowerApps/apps/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"></iframe>
```

<span data-ttu-id="f3542-228">Et la feuille de style CSS est la suivante :</span><span class="sxs-lookup"><span data-stu-id="f3542-228">And the CSS is:</span></span>

```css
#embed-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 94%;
    height: 95%;
    padding-left: 20px;
    padding-right: 20px;
    padding-top: 10px;
    padding-bottom: 10px;
    border-style: none;
}
```

## <a name="adaptive-card-or-adaptive-card-bot-card-attachment"></a><span data-ttu-id="f3542-229">Carte adaptative ou pièce jointe de carte de carte adaptative</span><span class="sxs-lookup"><span data-stu-id="f3542-229">Adaptive card or Adaptive card bot card attachment</span></span>

<span data-ttu-id="f3542-230">Comme indiqué ci-dessus, en fonction de la façon dont vous appelez `card` votre, vous devrez utiliser une carte adaptative ou une pièce jointe de carte de carte de carte adaptative (qui n’est qu’une carte adaptative dans un objet Attachment).</span><span class="sxs-lookup"><span data-stu-id="f3542-230">As we noted above, depending on how you're invoking your `card` you'll need to use either an Adaptive card, or an Adaptive card bot card attachment (which is just an Adaptive card wrapped in an attachment object).</span></span>

<span data-ttu-id="f3542-231">Lorsque vous appelez à partir d’un onglet, vous devez utiliser une carte adaptative.</span><span class="sxs-lookup"><span data-stu-id="f3542-231">When you're invoking from a tab, you'll need to use an adaptive card.</span></span> <span data-ttu-id="f3542-232">Voici un exemple très simple :</span><span class="sxs-lookup"><span data-stu-id="f3542-232">Here's a very simple example:</span></span>

```json
{
    "type": "AdaptiveCard",
    "body": [
        {
            "type": "TextBlock",
            "text": "Here is a ninja cat:"
        },
        {
            "type": "Image",
            "url": "http://adaptivecards.io/content/cats/1.png",
            "size": "Medium"
        }
    ],
    "version": "1.0"
}
```

<span data-ttu-id="f3542-233">Lorsque vous appelez à partir d’un robot, vous devez utiliser une pièce jointe de carte de carte adaptative comme dans l’exemple ci-dessous :</span><span class="sxs-lookup"><span data-stu-id="f3542-233">When you're invoking from a bot you'll need to use an Adaptive card bot card attachment as in the example below:</span></span>

```json
{
    "contentType": "application/vnd.microsoft.card.adaptive",
    "content": {
        "type": "AdaptiveCard",
        "body": [
            {
                "type": "TextBlock",
                "text": "Here is a ninja cat:"
            },
            {
                "type": "Image",
                "url": "http://adaptivecards.io/content/cats/1.png",
                "size": "Medium"
            }
        ],
        "version": "1.0"
    }
}
```

<span data-ttu-id="f3542-234">Vous devez garder à l’esprit si vous appelez un module de tâches contenant une carte adaptative à partir d’un bot ou d’un onglet.</span><span class="sxs-lookup"><span data-stu-id="f3542-234">You'll need to remember whether you are invoking a task module containing an Adaptive card from a bot or a tab.</span></span>

## <a name="task-module-deep-link-syntax"></a><span data-ttu-id="f3542-235">Syntaxe des liens détaillés des modules de tâches</span><span class="sxs-lookup"><span data-stu-id="f3542-235">Task module deep link syntax</span></span>

<span data-ttu-id="f3542-236">Un lien détaillé de module de tâches est simplement une sérialisation de l' [objet TaskInfo](#the-taskinfo-object) avec deux autres informations, `APP_ID` et éventuellement : `BOT_APP_ID`</span><span class="sxs-lookup"><span data-stu-id="f3542-236">A task module deep link is just a serialization of the [TaskInfo object](#the-taskinfo-object) with two other pieces of information, `APP_ID` and optionally the `BOT_APP_ID`:</span></span>

`https://teams.microsoft.com/l/task/APP_ID?url=<TaskInfo.url>&height=<TaskInfo.height>&width=<TaskInfo.width>&title=<TaskInfo.title>&completionBotId=BOT_APP_ID`

`https://teams.microsoft.com/l/task/APP_ID?card=<TaskInfo.card>&height=<TaskInfo.height>&width=<TaskInfo.width>&title=<TaskInfo.title>&completionBotId=BOT_APP_ID`

<span data-ttu-id="f3542-237">Voir [objet TaskInfo](#the-taskinfo-object) pour les types de données et les valeurs autorisées `<TaskInfo.url>`pour `<TaskInfo.card>`, `<TaskInfo.height>`, `<TaskInfo.width>`, et `<TaskInfo.title>`.</span><span class="sxs-lookup"><span data-stu-id="f3542-237">See [TaskInfo object](#the-taskinfo-object) for the data types and allowable values for `<TaskInfo.url>`, `<TaskInfo.card>`, `<TaskInfo.height>`, `<TaskInfo.width>`, and `<TaskInfo.title>`.</span></span>

> [!TIP]
> <span data-ttu-id="f3542-238">Assurez-vous que l’URL Encode le lien profond, en `card` particulier lorsque vous utilisez le paramètre (par exemple, la [ `encodeURI()` fonction](https://www.w3schools.com/jsref/jsref_encodeURI.asp)de JavaScript).</span><span class="sxs-lookup"><span data-stu-id="f3542-238">Be sure to URL encode the deep link, especially when using the `card` parameter (for example, JavaScript's [`encodeURI()` function](https://www.w3schools.com/jsref/jsref_encodeURI.asp)).</span></span>

<span data-ttu-id="f3542-239">Voici les informations sur `APP_ID` et : `BOT_APP_ID`</span><span class="sxs-lookup"><span data-stu-id="f3542-239">Here's the information on `APP_ID` and `BOT_APP_ID`:</span></span>

| <span data-ttu-id="f3542-240">Valeur</span><span class="sxs-lookup"><span data-stu-id="f3542-240">Value</span></span> | <span data-ttu-id="f3542-241">Type</span><span class="sxs-lookup"><span data-stu-id="f3542-241">Type</span></span> | <span data-ttu-id="f3542-242">Obligatoire ?</span><span class="sxs-lookup"><span data-stu-id="f3542-242">Required?</span></span> | <span data-ttu-id="f3542-243">Description</span><span class="sxs-lookup"><span data-stu-id="f3542-243">Description</span></span> |
| --- | --- | --- | --- |
| `APP_ID` | <span data-ttu-id="f3542-244">string</span><span class="sxs-lookup"><span data-stu-id="f3542-244">string</span></span> | <span data-ttu-id="f3542-245">Oui</span><span class="sxs-lookup"><span data-stu-id="f3542-245">Yes</span></span> | <span data-ttu-id="f3542-246">[ID](~/resources/schema/manifest-schema.md#id) de l’application appelant le module de tâches.</span><span class="sxs-lookup"><span data-stu-id="f3542-246">The [id](~/resources/schema/manifest-schema.md#id) of the app invoking the task module.</span></span> <span data-ttu-id="f3542-247">Le [tableau validDomains](~/resources/schema/manifest-schema.md#validdomains) dans le manifeste de `APP_ID` doit contenir le domaine pour `url` si `url` se trouve dans l’URL.</span><span class="sxs-lookup"><span data-stu-id="f3542-247">The [validDomains array](~/resources/schema/manifest-schema.md#validdomains) in the manifest for `APP_ID` must contain the domain for `url` if `url` is in the URL.</span></span> <span data-ttu-id="f3542-248">(L’ID de l’application est déjà connu lorsqu’un module de tâche est appelé à partir d’un onglet ou d’un bot, c’est pourquoi `TaskInfo`il n’est pas inclus dans.)</span><span class="sxs-lookup"><span data-stu-id="f3542-248">(The app ID is already known when a task module is invoked from a tab or a bot, which is why it's not included in `TaskInfo`.)</span></span> |
| `BOT_APP_ID` | <span data-ttu-id="f3542-249">string</span><span class="sxs-lookup"><span data-stu-id="f3542-249">string</span></span> | <span data-ttu-id="f3542-250">Non</span><span class="sxs-lookup"><span data-stu-id="f3542-250">No</span></span> | <span data-ttu-id="f3542-251">Si une valeur `completionBotId` est spécifiée, l' `result` objet est envoyé par le biais d' `task/submit invoke` un message au bot spécifié.</span><span class="sxs-lookup"><span data-stu-id="f3542-251">If a value for `completionBotId` is specified, the `result` object is sent via a a `task/submit invoke` message to the specified bot.</span></span> <span data-ttu-id="f3542-252">`BOT_APP_ID`doit être spécifié en tant que bot dans le manifeste de l’application, autrement dit, vous ne pouvez pas simplement l’envoyer à un bot.</span><span class="sxs-lookup"><span data-stu-id="f3542-252">`BOT_APP_ID` must be specified as a bot in the app's manifest, i.e. you can't just send it to any bot.</span></span> |

<span data-ttu-id="f3542-253">Notez qu’il est valide pour `APP_ID` le `BOT_APP_ID` même nom et, dans de nombreux cas, s’il s’agit d’un robot, car il est recommandé de l’utiliser comme ID d’une application s’il en existe un.</span><span class="sxs-lookup"><span data-stu-id="f3542-253">Note that it's valid for `APP_ID` and `BOT_APP_ID` to be the same, and in many cases will be if an app has a bot since it's recommended to use that as an app's ID if there is one.</span></span>

## <a name="keyboard-and-accessibility-guidelines"></a><span data-ttu-id="f3542-254">Instructions pour le clavier et l’accessibilité</span><span class="sxs-lookup"><span data-stu-id="f3542-254">Keyboard and accessibility guidelines</span></span>

<span data-ttu-id="f3542-255">Avec les modules de tâches HTML/JavaScript, il est de votre responsabilité de s’assurer que le module de tâches de votre application peut être utilisé avec un clavier.</span><span class="sxs-lookup"><span data-stu-id="f3542-255">With HTML/JavaScript-based task modules it is your responsibility to ensure your app's task module can be used with a keyboard.</span></span> <span data-ttu-id="f3542-256">Les programmes de lecteur d’écran dépendent également de la possibilité de naviguer à l’aide du clavier.</span><span class="sxs-lookup"><span data-stu-id="f3542-256">Screen reader programs also depend on the ability to navigate using the keyboard.</span></span> <span data-ttu-id="f3542-257">Pour des raisons pratiques, cela signifie deux choses :</span><span class="sxs-lookup"><span data-stu-id="f3542-257">As a practical matter this means two things:</span></span>

1. <span data-ttu-id="f3542-258">Utilisation de l' [attribut TabIndex](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/tabindex) dans vos balises HTML pour contrôler les éléments qui peuvent être concentrés et si/là où ils participent à la navigation séquentielle du clavier (généralement avec les touches <kbd>Tab</kbd> et <kbd>MAJ-TAB</kbd> ).</span><span class="sxs-lookup"><span data-stu-id="f3542-258">Using the [tabindex attribute](https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/tabindex) in your HTML tags to control which elements can be focused and if/where it participates in sequential keyboard navigation (usually with the <kbd>Tab</kbd> and <kbd>Shift-Tab</kbd> keys).</span></span>
2. <span data-ttu-id="f3542-259">Gestion de la touche <kbd>Échap</kbd> dans le code JavaScript pour votre module de tâches.</span><span class="sxs-lookup"><span data-stu-id="f3542-259">Handling the <kbd>Esc</kbd> key in the JavaScript for your task module.</span></span> <span data-ttu-id="f3542-260">Voici un exemple de code illustrant la procédure :</span><span class="sxs-lookup"><span data-stu-id="f3542-260">Here's a code sample showing how to do this:</span></span>

  ```javascript
  // Handle the Esc key
  document.onkeyup = function(event) {
  if ((event.key === 27) || (event.key === "Escape")) {
    microsoftTeams.submitTask(null); // this will return an err object to the completionHandler()
    }
  }
  ```

<span data-ttu-id="f3542-261">Microsoft teams garantit que la navigation au clavier fonctionne correctement à partir de l’en-tête du module de tâches dans votre code HTML et inversement.</span><span class="sxs-lookup"><span data-stu-id="f3542-261">Microsoft Teams will ensure that keyboard navigation works properly from the task module header into your HTML and vice-versa.</span></span>

## <a name="task-module-samples"></a><span data-ttu-id="f3542-262">Exemples de modules de tâches</span><span class="sxs-lookup"><span data-stu-id="f3542-262">Task module samples</span></span>

* [<span data-ttu-id="f3542-263">Exemple node. js/dactylographié</span><span class="sxs-lookup"><span data-stu-id="f3542-263">Node.js/TypeScript sample</span></span>](https://github.com/OfficeDev/microsoft-teams-sample-task-module-nodejs)
* [<span data-ttu-id="f3542-264">Exemple de/.NET C#</span><span class="sxs-lookup"><span data-stu-id="f3542-264">C#/.NET sample</span></span>](https://github.com/OfficeDev/microsoft-teams-sample-task-module-csharp)
